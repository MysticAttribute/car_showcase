/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
      domains: ["cdn.imagin.studio", "api.unsplash.com", "source.unsplash.com", "api.pexels.com", "images.pexels.com"]
  }
}

module.exports = nextConfig